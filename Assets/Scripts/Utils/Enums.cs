using UnityEngine;

namespace Utils
{
        public enum PlayerType
        {
            Fire,Water

        }

        public enum GameState
        {
            Title, Playing, Paused, GameOver, GameClear
        }
}
