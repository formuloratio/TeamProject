using UnityEngine;

namespace Utils
{
        public enum PlayerType
        {
            Fire,Water

        }

        public enum GameState
        {
            Run, Playing, Pause, End
        }
}
